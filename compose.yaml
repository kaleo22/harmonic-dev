services:
  ros2_jammy_sim:
    user: "0:0"
    build:
      context: ./
      dockerfile: ./DockerFiles/Dockerfile
      args:
        - BASE_IMAGE=osrf/ros
        - BASE_TAG=jazzy-desktop-full
        - DISTRO=jazzy
        - WORKSPACE=docker_simulation_ws
        - USERNAME=ros
        - USER_UID=1000
        - USER_GID=1000

    container_name: ros2_jammy_simulator
    networks:
      - ros2_internal

    stdin_open: true
    tty: true

    # wichtig für Gazebo/Qt/Rendering
    shm_size: "2gb"

    environment:
      - ROS_DOMAIN_ID=0
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - GZ_VERSION=harmonic

      # NVIDIA Container Runtime
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all, graphics,display,utility,compute

      # VNC Settings
      - DISPLAY=:1
      - VNC_GEOMETRY=1920x1080
      - VNC_DEPTH=24
      - VNC_PASSWORD=change-me

      #VGL Settings
      - VGL_DISPLAY=:0             # host Xorg (:0 via /tmp/.X11-unix/X0)

    ports:
      - "5901:5901"   # VNC (TurboVNC)

    volumes:
      - ~/.bash_history:/home/ros/.bash_history
      - ./docker_simulator:/home/ros/docker_simulation_ws
      - /tmp/.X11-unix/X0:/tmp/.X11-unix/X0:rw
      - /tmp/xorg0.Xauthority:/tmp/xorg0.Xauthority:ro

    # kein privileged nötig, kein /dev/nvidia* Mount nötig, kein /tmp/.X11-unix Mount nötig
    runtime: nvidia

networks:
  ros2_internal:
    driver: bridge
